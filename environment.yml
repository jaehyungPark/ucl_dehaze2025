name: ucl
channels:
- pytorch
- defaults
dependencies:
- python=3.6
- pytorch=1.6.0
- torchvision=0.7.0
- scipy
- pip:
  - dominate==2.4.0
  - Pillow==6.1.0
  - numpy==1.16.4
  - visdom==0.1.8
  - packaging
  - GPUtil==1.4.0

  # conda create -n ucl python=3.6 
  ## -> and pip install dependencies.
  # OR
  # # conda env create -f environment.yml